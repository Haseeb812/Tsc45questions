let friendsNames = ["Haseeb", "Rizwan", "Badar"];
let message =
  "My heart feels cared for, anytime I recall that I have a lovely person like you as a friend.";
for (let i = 0; i < friendsNames.length; i++) {
  console.log(`hello ${friendsNames[i]} ${message}`);
}
