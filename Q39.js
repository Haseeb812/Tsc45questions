"use strict";
function city_country(city, country) {
    return `${city}, ${country}`;
}
// Example usage:
let cityName = "Lahore";
let countryName = "Pakistan";
let result = city_country(cityName, countryName);
console.log(result); // Outputs: Lahore, Pakistan
